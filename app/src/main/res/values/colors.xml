<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Home-screen widget. Brand RoadBlue (matches the Compose primary) with
         a white foreground; fixed rather than themed so it reads the same on
         any launcher wallpaper. -->
    <color name="widget_bg">#FF2F5FA8</color>
    <color name="widget_fg">#FFFFFFFF</color>
</resources>